---
import { Icon } from 'astro-icon/components'

const styles = `md:w-[277px] md:h-[50px] w-[220px] h-[40px]`
---

<Icon name="logo" class={`dark:hidden ${styles}`} />
<Icon name="logo_white" class={`dark:block hidden ${styles}`} />