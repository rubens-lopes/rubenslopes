---
import { Icon } from 'astro-icon/components'

const { title, url, icon, class: css } = Astro.props
---

<a {title} aria-label={title} class={css} href={url}>
  <Icon name={icon} class="size-6 md:size-10" />
</a>
